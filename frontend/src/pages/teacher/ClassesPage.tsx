import React from 'react';
import { Typography } from '@mui/material';

const ClassesPage: React.FC = () => {
  return <Typography variant="h4">Teacher Classes</Typography>;
};

export default ClassesPage;
