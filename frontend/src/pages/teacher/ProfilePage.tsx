import React from 'react';
import { Typography } from '@mui/material';

const ProfilePage: React.FC = () => {
  return <Typography variant="h4">Teacher Profile</Typography>;
};

export default ProfilePage;
