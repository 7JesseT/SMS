import React from 'react';
import { Typography } from '@mui/material';

const TeacherDashboard: React.FC = () => {
  return <Typography variant="h4">Teacher Dashboard</Typography>;
};

export default TeacherDashboard;
