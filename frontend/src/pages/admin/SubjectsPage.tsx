import React from 'react';
import { Typography } from '@mui/material';

const SubjectsPage: React.FC = () => {
  return <Typography variant="h4">Subjects</Typography>;
};

export default SubjectsPage;
