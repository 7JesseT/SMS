import React from 'react';
import { Typography } from '@mui/material';

const ComplaintsPage: React.FC = () => {
  return <Typography variant="h4">Student Complaints</Typography>;
};

export default ComplaintsPage;
