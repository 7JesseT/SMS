import React from 'react';
import { Typography } from '@mui/material';

const GradesPage: React.FC = () => {
  return <Typography variant="h4">Student Grades</Typography>;
};

export default GradesPage;
