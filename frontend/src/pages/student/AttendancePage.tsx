import React from 'react';
import { Typography } from '@mui/material';

const AttendancePage: React.FC = () => {
  return <Typography variant="h4">Student Attendance</Typography>;
};

export default AttendancePage;
