import React from 'react';
import { Typography } from '@mui/material';

const StudentDashboard: React.FC = () => {
  return <Typography variant="h4">Student Dashboard</Typography>;
};

export default StudentDashboard;
